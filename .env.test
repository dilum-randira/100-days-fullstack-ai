# Root E2E test env (Vitest)
# Individual services load their own .env.test if needed.
NODE_ENV=test
LOG_LEVEL=error

# Services (used by gateway tests if hitting real servers)
AUTH_SERVICE_URL=http://localhost:4000
INVENTORY_SERVICE_URL=http://localhost:3000
ANALYTICS_SERVICE_URL=http://localhost:5000

# Dummy secrets for local test signing (if needed)
JWT_ACCESS_SECRET=test-access-secret
JWT_REFRESH_SECRET=test-refresh-secret
JWT_SECRET=test-gateway-secret

# Mongo is provided by mongodb-memory-server per service vitest.setup.ts
