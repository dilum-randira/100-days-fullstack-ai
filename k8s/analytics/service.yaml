apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: backend
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 3000
      targetPort: http
